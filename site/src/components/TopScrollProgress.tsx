import { ScrollProgress } from "@/components/ui/scroll-progress";

export default function TopScrollProgress() {
  return (
    <ScrollProgress className="top-0 z-50 h-0.5 from-amber/80 via-amber to-amber/80" />
  );
}
